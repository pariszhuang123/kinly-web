#!/usr/bin/env node
/**
 * Contract validation now lives in the kinly-contracts repo.
 * This shim remains to avoid breaking existing task runners.
 */

console.log(
  "check_contracts skipped: contract validation now runs in the kinly-contracts codebase."
);
